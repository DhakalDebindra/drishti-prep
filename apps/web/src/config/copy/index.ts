/**
 * All editable interface text.
 *
 * Split by audience, then by surface — one small file per place a string
 * appears, so changing wording means opening one obvious file rather than
 * searching a large one. See README.md for the map.
 *
 * Import from "@/config/copy"; the folder layout stays a detail of this folder.
 */

export * from "./user";
export * from "./admin";
