/**
 * Playback controls stay in English — interface chrome, like the navigation,
 * and short English verbs read more clearly on a button.
 */
export const AudioCopy = {
  listen: "Listen",
  stop: "Stop",
  loading: "Preparing…",
  retry: "Try again",
} as const;
