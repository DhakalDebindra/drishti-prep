/** The /search page shell: title and the empty state before any query. */
export const SearchPageCopy = {
  /** Shown instead of the query when nothing has been searched yet. */
  titleFallback: "Search DrishtiPrep",
  /** Announced before the query so a screen reader gives it context. */
  resultsForPrefix: "Search results for ",
  emptyPrompt: "Search practice sets, topics and questions",
  emptyHintNe: "नेपाली वा अङ्ग्रेजी – जुनसुकै भाषामा खोज्न सकिन्छ।",
} as const;
